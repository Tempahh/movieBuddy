export class RegisterDto {
  username: string;
  email: string;
  password: string;
}
// The RegisterDto class defines the structure of the data that the client sends to the server when registering a new user. The class consists of the following fields:
